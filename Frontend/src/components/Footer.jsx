const Footer = () => {
    return (
        <footer className="app-footer">
            <span>Quizzle study workspace</span>
            <span>Question banks, tests, OCR upload, and progress review</span>
        </footer>
    );
};

export default Footer;
